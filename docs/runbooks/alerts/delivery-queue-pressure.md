# Durable Delivery Queue Pressure

Use RabbitMQ Management on `http://127.0.0.1:15672` in the lab plus container logs/health.
Hold delivery with `pnpm lab:delivery:hold`, resume with `pnpm lab:delivery:resume`, and drain
with `pnpm lab:delivery:drain`. Purge only with explicit confirmation.
